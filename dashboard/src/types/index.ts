/**
 * Type Definitions Index
 * 
 * Re-exports all types from the audit module for convenient importing.
 * 
 * Usage:
 *   import { Finding, Evidence, AuditStatus } from '@/types';
 */

export * from './audit';
